#include "solution.h"
#include <string>

void solve(std::istream& in, std::ostream& out) {
  // Example: Echo functionality
  std::string s;
  if (std::getline(in, s)) {
      out << s << "\n";
  }
}
